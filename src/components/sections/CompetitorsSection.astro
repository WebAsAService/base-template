---
// Competitors vs Crown Section Component
import { Image } from "astro:assets";
import bulletList from "../../assets/bullet-list.svg";
import divisor from "../../assets/divisor.svg";
---

<section id="competitors" class="py-16 bg-primary-900 text-white">
  <div class="container mx-auto px-4">
    <div class="mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4 text-secondary-500">
        We Connect the Dots <br> in Ways Others Can’t
      </h2>
      <p class="text-xl text-white max-w-3xl mx-auto">
        We’ve done the math so you don’t have to.<br />
        You won’t see any names here, but if you’ve talked to other firms, you’ll
        recognize the pattern.
      </p>
    </div>

    <div class="flex flex-col md:flex-row items-center mb-16 gap-8">
      <div class="w-full md:w-1/2">
        <img
          src="/images/clients/crown/alaska-s-beautiful-landscape.png"
          alt="Alaska's Beautiful Landscape"
          class="w-full rounded-lg shadow-lg"
        />
      </div>
      <div class="w-full md:w-1/2">
        <h3 class="text-xl font-bold mb-4 text-secondary-500">
          Here's what most teams miss — and how Crown stacks up.
        </h3>
        <h4 class="text-lg font-semibold text-white mb-3">
          Key Competitive Advantages
        </h4>
        <ul class="space-y-3">
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white"
              >3–8x more operational hours than lean back-office firms</span
            >
          </li>
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white"
              >Deep execution: not just dashboards, but decisions</span
            >
          </li>
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white"
              >Built-in support across Finance, HR, and Systems</span
            >
          </li>
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white"
              >Costs less than building an internal team</span
            >
          </li>
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white"
              >Fully scalable, no need to hire full-time</span
            >
          </li>
          <li class="flex items-start">
            <div class="mr-3 mt-0.5">
              <Image
                src={bulletList}
                alt="Bullet point"
                width={20}
                height={20}
              />
            </div>
            <span class="text-white">No gaps, no outsourcing chaos</span>
          </li>
        </ul>
      </div>
    </div>

    <div class="mt-16 mb-8">
      <div class="grid grid-cols-4 gap-4 text-secondary-500 text-xl">
        <div class="col-span-2 text-left font-bold">Services</div>
        <div class="font-bold">Most Providers</div>
        <div class="font-bold">Us</div>
      </div>

      <!-- Cost Row -->
      <div class="mt-4">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Cost</div>
          <div class="">$40k - $80k /mo</div>
          <div class="">$5k - $20k /mo</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Number of Resources Row -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Number of Resources</div>
          <div class="">3 - 5</div>
          <div class="">4 - 10</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Expertise Row -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Expertise</div>
          <div class="">Mixed, often junior</div>
          <div class="">Subject Matter Experts</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Flexibility Row -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Flexibility</div>
          <div class="">Fixed staff</div>
          <div class="">Scalable, plug-in resources</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Onboarding Rows -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Onboarding</div>
          <div class="">1-3 months</div>
          <div class="">Go live in under 30 days</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Additional Onboarding Rows (repeated in screenshot) -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Onboarding</div>
          <div class="">1-3 months</div>
          <div class="">Go live in under 30 days</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Additional Onboarding Rows (repeated in screenshot) -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Onboarding</div>
          <div class="">1-3 months</div>
          <div class="">Go live in under 30 days</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>

      <!-- Additional Onboarding Rows (repeated in screenshot) -->
      <div class="mt-6">
        <div class="grid grid-cols-4 gap-4 text-white items-center">
          <div class="col-span-2 text-left">Onboarding</div>
          <div class="">1-3 months</div>
          <div class="">Go live in under 30 days</div>
        </div>
        <Image src={divisor} alt="Divider" class="w-full my-4" />
      </div>
    </div>

    <div
      class="text-center mt-16 mx-auto bg-primary-800 rounded-lg p-8 shadow-lg"
    >
      <h3 class="text-2xl font-bold mb-4 text-secondary-500">
        Want the full breakdown?
      </h3>
      <p class="text-lg text-white mb-6">
        We've benchmarked every major competitor and built a detailed
        comparison.
        <br />
        👉 <em>We'll show it on the call — names included.</em>
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <div class="relative">
          <input
            type="email"
            placeholder="Your email"
            class="w-full text-black sm:w-64 px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-500 focus:border-accent-500"
          />
        </div>
        <button
          class="bg-primary-600 hover:bg-primary-700 text-white font-medium py-2 px-6 rounded-lg transition-colors"
        >
          Request the Comparison
        </button>
      </div>
    </div>
  </div>
</section>
